name: Generate AI Robot Animation

on:
  schedule:
    - cron: "0 0 * * *" # Her gece saat 00:00'da çalışır
  workflow_dispatch: # İstersen GitHub üzerinden manuel de tetikleyebilirsin

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repoyu Klonla
        uses: actions/checkout@v3

      - name: Python Ortamını Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: SVG Scriptini Çalıştır
        run: python generate_animation.py

      - name: Yeni Animasyonu Repoya Pushla (Değişiklik varsa)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add robot-animasyon.svg
          git commit -m "🤖 Robot animasyonu güncellendi" || exit 0
          git push
